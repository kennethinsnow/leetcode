package google;
import binarytree.TreeNode;

public class string2tree {
	// sample string (A(B(C)(D))(E)(F))
	public TreeNode getTree(String inp){
		TreeNode ret = new TreeNode(0);
		return ret;
	}
}
